# Base

Create default collections:

- bookmark_services
    - name: string
- bookmark_items
    - name: string
    - url: url
    - service: bookmark_services(single)

Create some services as only needed

- GitHub
- Confluence
- etc
